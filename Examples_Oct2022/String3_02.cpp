/*#include <iostream>
#include <string>

/*void find_num_denom(int& num, int& denom, std::string input) {
    int slash_index = input.find("/");
    num = std::stoi(input.substr(0, slash_index));
    denom = std::stoi(input.substr(slash_index + 1, input.length()));
}

int main() {
    int n, d;
    find_num_denom(n, d, "23/52");
    std::cout << n << "  " << d << "\n";
    return 0;
}

void str_to_float(float& , float& , std::string );

int main() {
    float n1, n2;
    str_to_float(n1, n2, "35/5");
    std::cout << " \n n1= " << n1;
    std::cout << "\n n2= " << n2;
    std::cout << "\n";
    return 0;
}

void str_to_float(float& n1_a, float& n2_a, std::string a) {
    float specialSymbal = a.find("/");
    n1_a = std::stof(a.substr(0, specialSymbal));
    n2_a = std::stof(a.substr(specialSymbal + 1, a.length()));
}*/